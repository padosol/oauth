spring:
  application:
    name: user-service

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${CLIENT_ID}
            client-secret:
            scope:
              - profile
              - email